spring:
  application:
    name: hm-gateway  #服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos地址
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml